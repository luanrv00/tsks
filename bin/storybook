#!/usr/bin/env sh
bin/download-node-image
docker compose -f docker-compose.storybook.yml up --build --force-recreate --abort-on-container-exit
docker rm -f tsks-web-storybook
docker rmi -f tsks-web-storybook
