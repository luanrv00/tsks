import {AddButton} from '.'

export default {
  component: AddButton,
  tags: ['autodocs'],
  title: 'buttons/AddButton',
}

export const Primary = {
  args: {
    value: 'click',
    onClick: () => {},
  },
}
