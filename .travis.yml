language: bash
services: 
  - docker
script:
  - docker-compose up --detach
  - docker-compose run svc bundle exec rake db:create
  - docker-compose run svc bundle exec rake db:migrate
  - docker-compose run svc bundle exec rake spec
